{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"outDir": "build",
		"noEmit": false,
		"declaration": true,
		"declarationMap": true
	},
	// Things not to transpile
	// TODO Adding test here causes it to: 'React' refers to a UMD global, but the current file is a module.
	"exclude": ["*.test.tsx", "build/*"]
}
